# DevLinks - 프로젝트 링크 모음 사이트 구현 계획

## Context

포트폴리오(jyc_portfolio)에 있는 7개 프로젝트의 링크(GitHub, Demo, App Store, Toss 미니앱)를 한곳에 모은 Linktree 스타일의 개발자 링크 사이트를 만듭니다. 포트폴리오의 디자인 아이덴티티(틸 컬러, Pretendard 폰트, Framer Motion 애니메이션)를 유지하면서, 웹/모바일 반응형으로 제작합니다.

---

## 기술 스택

| 항목 | 선택 | 비고 |
|------|------|------|
| 프레임워크 | Next.js 16 + React 19 + TypeScript | 포트폴리오와 동일 |
| 스타일링 | Tailwind CSS 4 | 포트폴리오와 동일 |
| 애니메이션 | Framer Motion 12 | FadeIn 컴포넌트 재사용 |
| 다크모드 | next-themes | 포트폴리오와 동일 |
| 폰트 | Pretendard (CDN) | 포트폴리오와 동일 |
| 컬러 | Primary #14b8a6, Light #5eead4, Dark #0d9488 | 포트폴리오와 동일 |

---

## 프로젝트 구조

```
jyc_devlinks/
├── src/
│   ├── app/
│   │   ├── globals.css          # 글로벌 스타일, 폰트, 테마 변수
│   │   ├── layout.tsx           # 루트 레이아웃 (ThemeProvider)
│   │   └── page.tsx             # 메인 페이지 (싱글 페이지)
│   ├── components/
│   │   ├── Header.tsx           # 상단 바 (로고 + 다크모드 토글)
│   │   ├── ProfileSection.tsx   # 프로필 영역 (이름, 역할, 소셜 링크)
│   │   ├── ProjectCard.tsx      # 개별 프로젝트 카드
│   │   ├── ProjectsGrid.tsx     # 프로젝트 카드 그리드 컨테이너
│   │   ├── Footer.tsx           # 하단 푸터
│   │   └── ui/
│   │       ├── FadeIn.tsx       # 스크롤 애니메이션 (포트폴리오에서 복사)
│   │       ├── ThemeToggle.tsx  # 다크/라이트 토글 (포트폴리오에서 복사)
│   │       ├── LinkButton.tsx   # 링크 버튼 (GitHub, Demo, Toss 등)
│   │       └── ScrollToTop.tsx  # 스크롤 투 탑 버튼
│   └── data/
│       └── links.ts             # 프로젝트/프로필 데이터 (단일 데이터 소스)
├── public/
│   └── (favicon 등)
├── package.json
├── tsconfig.json
├── postcss.config.mjs
└── next.config.ts
```

---

## 페이지 레이아웃

```
┌─────────────────────────────────────┐
│  Header (로고 "DevLinks" + 🌙 토글) │
├─────────────────────────────────────┤
│                                     │
│  ProfileSection                     │
│  ┌─────────────────────────────┐    │
│  │  </> 아이콘                  │    │
│  │  조영찬                      │    │
│  │  Developer                   │    │
│  │  끊임없이 성장하는 개발자      │    │
│  │  [GitHub] [LinkedIn] [Email] │    │
│  └─────────────────────────────┘    │
│                                     │
├─────────────────────────────────────┤
│                                     │
│  Projects 섹션 제목                  │
│                                     │
│  ┌──────────┐  ┌──────────┐         │
│  │ 코레일    │  │ FootHub  │         │
│  │ 동행열차  │  │          │         │
│  │ [GitHub] │  │[AppStore]│         │
│  └──────────┘  │[GitHub]  │         │
│                └──────────┘         │
│  ┌──────────┐  ┌──────────┐         │
│  │ 충주 균형 │  │ 두바이   │         │
│  │ 발전     │  │ 왕자     │         │
│  │[Demo]    │  │[Demo]    │         │
│  │[GitHub]  │  │[Toss]    │         │
│  └──────────┘  │[GitHub]  │         │
│                └──────────┘         │
│  ... (나머지 프로젝트들)             │
│                                     │
├─────────────────────────────────────┤
│  Footer                             │
└─────────────────────────────────────┘
```

**반응형**: 모바일 1열 → md(768px) 이상 2열 그리드

---

## 컴포넌트별 상세 설계

### 1. ProfileSection
- **배경**: 틸 그래디언트 (`#0d9488` → `#14b8a6`) + 그리드 패턴 오버레이
- **내용**: `</>` 개발자 아이콘 → 이름 → 역할 → 태그라인 → 소셜 링크 (원형 아이콘 버튼)
- **애니메이션**: 순차적 FadeIn (stagger delay 0.1s 간격)
- **다크모드**: 그래디언트 → `#0d9488` → `#111827`

### 2. ProjectCard
- **라이트모드**: `bg-white shadow-md border border-gray-100 rounded-2xl`
- **다크모드**: `bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl` (글라스모피즘)
- **카드 내부**: 기간 뱃지 + 수상 뱃지(있으면) → 프로젝트 제목 → 부제목 → 기술 스택 태그 → 링크 버튼들
- **호버**: `scale(1.02), y: -4` / **탭**: `scale(0.98)`

### 3. LinkButton
- **GitHub**: `bg-gray-900 text-white` + GitHub 아이콘
- **Demo**: `bg-teal-500 text-white` + 외부링크 아이콘
- **App Store**: `bg-black text-white` + Apple 아이콘
- **Toss 미니앱**: `bg-[#0064FF] text-white` + Toss 텍스트
- **호버**: `scale(1.05)` / **탭**: `scale(0.95)`
- 모든 링크 `target="_blank" rel="noopener noreferrer"`

### 4. Header
- 고정(fixed) 상단 바, 스크롤 시 backdrop-blur 배경
- 왼쪽: "DevLinks" 로고 (틸 색상 점 강조)
- 오른쪽: ThemeToggle

### 5. Footer
- 다크 배경 (`#111`)
- 저작권 텍스트 + 포트폴리오 링크

---

## 데이터 구조 (`src/data/links.ts`)

```typescript
type LinkType = "github" | "demo" | "appstore" | "toss";

interface ProjectLink {
  type: LinkType;
  url: string;
  label: string;
}

interface Project {
  id: number;
  title: string;
  subtitle: string;
  techStack: string[];
  links: ProjectLink[];
  award?: string;
  period: string;
}

interface ProfileData {
  name: string;
  nameEn: string;
  role: string;
  tagline: string;
  socials: SocialLink[];
}
```

7개 프로젝트 데이터를 포트폴리오의 `portfolio.ts`에서 가져와 링크 중심으로 재구성합니다.

---

## 포트폴리오에서 재사용할 파일

| 파일 | 경로 | 용도 |
|------|------|------|
| FadeIn.tsx | `jyc_portfolio/src/components/ui/FadeIn.tsx` | 스크롤 애니메이션 래퍼 (그대로 복사) |
| ThemeToggle.tsx | `jyc_portfolio/src/components/ui/ThemeToggle.tsx` | 다크모드 토글 (그대로 복사) |
| globals.css | `jyc_portfolio/src/app/globals.css` | 폰트, CSS 변수, Tailwind 설정 (필요한 부분만 추출) |

---

## 구현 순서

### Phase 1: 프로젝트 초기화
1. `create-next-app`으로 프로젝트 생성 (TypeScript, Tailwind, App Router)
2. `framer-motion`, `next-themes` 의존성 추가
3. `postcss.config.mjs`, `tsconfig.json` 설정

### Phase 2: 스타일 기반
4. `globals.css` 작성 (Pretendard 폰트, CSS 변수, 다크모드 variant, 스크롤바)

### Phase 3: 데이터
5. `src/data/links.ts` 작성 (프로필 + 7개 프로젝트 데이터 + 기술태그 색상)

### Phase 4: UI 컴포넌트
6. `FadeIn.tsx`, `ThemeToggle.tsx` 복사
7. `LinkButton.tsx`, `ScrollToTop.tsx` 작성

### Phase 5: 페이지 컴포넌트
8. `Header.tsx` 작성
9. `ProfileSection.tsx` 작성
10. `ProjectCard.tsx` 작성
11. `ProjectsGrid.tsx` 작성
12. `Footer.tsx` 작성

### Phase 6: 페이지 조립
13. `layout.tsx` 작성 (ThemeProvider, 메타데이터)
14. `page.tsx` 작성 (모든 컴포넌트 조합)

### Phase 7: 검증
15. `npm run dev`로 로컬 실행 확인
16. 반응형 동작 확인 (모바일/데스크톱)
17. 다크모드 전환 확인
18. 모든 링크 정상 작동 확인

---

## 검증 방법

1. **`npm run dev`** 실행하여 로컬에서 페이지 확인
2. **반응형 테스트**: 브라우저 DevTools에서 모바일(375px), 태블릿(768px), 데스크톱(1024px+) 확인
3. **다크모드**: 토글 클릭하여 라이트/다크 전환 확인
4. **링크**: 모든 프로젝트 링크(GitHub, Demo, App Store, Toss) 클릭 시 새 탭 열림 확인
5. **애니메이션**: 스크롤 시 FadeIn 동작, 카드/버튼 호버 효과 확인
6. **`npm run build`** 실행하여 빌드 에러 없음 확인
